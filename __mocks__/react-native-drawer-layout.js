// __mocks__/react-native-drawer-layout.js
export const DrawerState = {
  UNDETERMINED: 'undetermined',
  DRAGGING: 'dragging',
  SETTLING: 'settling',
};

const DrawerLayout = (props) => {
  // Retourne un simple View ou un div simulé pour Jest
  return null;
};

export default DrawerLayout;
